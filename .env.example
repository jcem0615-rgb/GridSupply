# Leave these blank to run GridSupply in local-first demo mode (Dexie/IndexedDB only).
# Fill them in to point the outbox sync layer at a real Supabase project.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
